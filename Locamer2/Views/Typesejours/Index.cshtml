@model IEnumerable<Locamer2.Models.Typesejour>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.libelle_typesejour)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.prix)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.libelle_typesejour)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.prix)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_typesejour }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_typesejour }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_typesejour })
        </td>*@
    </tr>
}

</table>
